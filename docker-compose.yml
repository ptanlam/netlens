services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - DB_PATH=/app/data/investments.db
      - APP_PASSWORD=changeme
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
