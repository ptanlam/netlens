services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      - DB_PATH=/app/data/investments.db
    volumes:
      - app-data:/app/data
    restart: unless-stopped

volumes:
  app-data:
